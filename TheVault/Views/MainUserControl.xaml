<UserControl x:Class="TheVault.Views.MainUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid x:Name="TopControls" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Row="0" Grid.Column="0"
                    x:Name="OpenFolderOrigin"
                    Style="{StaticResource FolderButton}"
                    Margin="5,5,5,1"
                    Command="{Binding OpenOriginFolder}"
                    Content="OPEN"/>

            <TextBox Grid.Row="0" Grid.Column="1" 
                     x:Name="OriginPath"
                     Margin="0,5,5,1"
                     Text="{Binding OriginPath}"
                     materialDesign:HintAssist.Hint="Path to origin folder">

            </TextBox>

            <Button Grid.Row="1" Grid.Column="0"
                    x:Name="OpenFolderDestination"
                    Style="{StaticResource FolderButton}"
                    Margin="5,1,5,5"
                    Command="{Binding OpenDestinationFolder}"
                    Content="OPEN"/>
            
            <TextBox Grid.Row="1" Grid.Column="1"  
                     x:Name="DestinationPath"
                     Margin="0,1,5,5"
                     Text="{Binding DestinationPath}"
                     materialDesign:HintAssist.Hint="Path to destination folder">

            </TextBox>
        </Grid>

        <ListView Grid.Row="1" Grid.Column="0" ItemsSource="{Binding DecryptedFiles}">
            <ListView.View>
                <GridView>
                    <GridViewColumn>
                        <CheckBox IsChecked="{Binding DecryptedAll, Mode=TwoWay}" />
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn
                        DisplayMemberBinding="{Binding FileName}"
                        Header="Filename" />
                    <GridViewColumn
                        DisplayMemberBinding="{Binding Path}"
                        Header="Path" />
                </GridView>
            </ListView.View>
        </ListView>
        <ListView Grid.Row="1" Grid.Column="1" ItemsSource="{Binding CryptedFiles}">
            <ListView.View>
                <GridView>
                    <GridViewColumn
                        DisplayMemberBinding="{Binding IsSelected}"
                        Header="Selected" />
                    <GridViewColumn
                        DisplayMemberBinding="{Binding FileName}"
                        Header="Filename" />
                    <GridViewColumn
                        DisplayMemberBinding="{Binding Path}"
                        Header="Path" />
                </GridView>
            </ListView.View>
        </ListView>
        
        <Grid x:Name="BottomControls" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    x:Name="StartServer"
                    Style="{StaticResource ServerButton}"
                    Margin="5,5,5,5"
                    Content="START SERVER"/>

            <Button Grid.Column="1"
                    x:Name="SendData"
                    Style="{StaticResource SendButton}"
                    Margin="0,5,5,5"
                    Content="SEND DATA"/>

            <TextBlock Grid.Column="2" 
                       TextAlignment="Right"
                       VerticalAlignment="Center"
                       Margin="0,0,5,0"
                       Style="{StaticResource ServerBlock}"
                       Text="{Binding ServerMessage}" />
        </Grid>
    </Grid>
</UserControl>
